% recopier ce fichier avec le nom `sessions_container.tex` pour le modifier,
% sinon il sera regénéré à chaque appel de `jdd_generator`.

%\event{événement}{0,0,1}{début -- fin}{orateur}

\block{ for session in sessions }
\block{ if loop.first or session.date.day != sessions[loop.index - 2].date.day }
\subsection*{\var{ session.date|date|capitalize }}

\block{ endif }
\input{\var{ session.file_name }}
%\newpage

\block{ endfor }

%\event{événement}{0,0,1}{début -- fin}{orateur}
